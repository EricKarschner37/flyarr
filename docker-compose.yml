services:
  postgres:
    restart: unless-stopped
    image: postgres:16-alpine
    container_name: flyarr-db
    environment:
      POSTGRES_USER: flyarr
      POSTGRES_PASSWORD: flyarr
      POSTGRES_DB: flyarr
    ports:
      - "5433:5432"
    volumes:
      - flyarr_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U flyarr -d flyarr"]
      interval: 5s
      timeout: 5s
      retries: 5
  flyarr:
    restart: unless-stopped
    build: .
    ports:
      - "9092:3000"
    environment:
      DATABASE_URL: postgresql://flyarr:flyarr@postgres:5432/flyarr
    depends_on:
      postgres:
        condition: service_healthy

volumes:
  flyarr_data:
